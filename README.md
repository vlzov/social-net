
# Simple Social Network Backend (Spring Boot)

[![Java](https://img.shields.io/badge/Java-17%2B-orange?logo=openjdk)](https://openjdk.org/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.x-brightgreen?logo=springboot)](https://spring.io/projects/spring-boot)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

Учебный проект, демонстрирующий создание базового бэкенда для социальной сети на Java и Spring Boot. Проект реализует простые CRUD-операции для управления пользователями и их постами через REST API.

Этот проект — отличная отправная точка для понимания основ:
*   Архитектуры веб-приложений (клиент-сервер, REST).
*   Фреймворка Spring Boot.
*   Работы с HTTP-методами (GET, POST, PUT, DELETE).
*   Структурирования контроллеров и сущностей.

## 📋 Содержание
- [Функциональность](#-функциональность)
- [Технологический стек](#-технологический-стек)
- [Структура проекта](#-структура-проекта)
- [Быстрый старт](#-быстрый-старт)
- [Использование API](#-использование-api)
- [Примеры запросов](#-примеры-запросов)
- [Цели обучения](#-цели-обучения)
- [Возможные улучшения](#-возможные-улучшения)
- [Лицензия](#-лицензия)

## 🚀 Функциональность

*   **Управление пользователями:** Создание, просмотр, удаление.
*   **Управление постами:** Добавление постов к пользователям.
*   **REST API:** Полностью соответствует принципам REST.
*   **Встроенный сервер:** Запускается на Apache Tomcat (порт 8080).
*   **In-Memory Хранение:** Данные хранятся в памяти приложения (список `List<User>`).

## 🛠 Технологический стек

*   **Java 17+**
*   **Spring Boot 3.x**
*   **Spring Web** (для создания REST-контроллеров)
*   **Apache Tomcat** (встроенный веб-сервер)
*   **Maven** (для сборки проекта)

## 📁 Структура проекта
```
src/main/java/vlzov/social/
├── User.java                # Сущность "Пользователь"
├── Post.java                # Сущность "Пост" (Record)
├── GreetingController.java  # Контроллер для примеров с GET-запросами
└── UserActivityController.java # Основной контроллер (CRUD для пользователей)
```

## ⚡ Быстрый старт

### Предварительные требования
1.  Установите **JDK 17** или выше.
2.  Установите **Maven** (или используйте обертку Maven Wrapper).
3.  Установите IDE (IntelliJ IDEA, VS Code, Eclipse) или используйте текстовый редактор.
4.  Установите API-клиент (**Postman**, **Insomnia** или используйте `curl`).

### Установка и запуск
1.  **Клонируйте репозиторий:**
    ```bash
    git clone git@github.com:vlzov/social-net.git
    cd social-net
    ```

2.  **Соберите и запустите проект:**
    ```bash
    # Используя Maven Wrapper (если есть)
    ./mvnw spring-boot:run

    # Или используя установленный Maven
    mvn spring-boot:run
    ```

3.  Приложение запустится на `http://localhost:8080`.

## 📡 Использование API

### Базовый URL
Все эндпоинты доступны по адресу: `http://localhost:8080`

---

### Эндпоинты `GreetingController` (`/greet`)
| Метод | Эндпоинт | Описание | Параметры |
|-------|----------|----------|-----------|
| `GET` | `/greet` | Простое приветствие | `?name=<имя>` (опционально) |
| `GET` | `/greet/full` | Полное приветствие | `?name=<имя>&surname=<фамилия>` |
| `GET` | `/greet/{name}`| Приветствие через PathVariable | `{name}` в пути |

---

### Эндпоинты `UserActivityController` (`/users`)
| Метод | Эндпоинт | Описание | Тело запроса (JSON) |
|-------|----------|----------|---------------------|
| `GET` | `/users` | Получить список всех пользователей | Не требуется |
| `GET` | `/users/{username}` | Получить пользователя по имени | Не требуется |
| `POST`| `/users` | Создать нового пользователя | `{"username": "string", "posts": []}` |
| `PUT` | `/users/{username}` | Добавить пост пользователю | `{"description": "string", "imageUrl": "string"}` |
| `DELETE`| `/users/{username}` | Удалить пользователя по имени | Не требуется |

## 🧪 Примеры запросов (через Postman/cURL)

### 1. Создание пользователя (POST)
```bash
curl -X POST http://localhost:8080/users \
  -H "Content-Type: application/json" \
  -d '{
    "username": "Tom",
    "posts": []
  }'
```

### 2. Получение всех пользователей (GET)
```bash
curl -X GET http://localhost:8080/users
```

### 3. Добавление поста пользователю (PUT)
```bash
curl -X PUT http://localhost:8080/users/Tom \
  -H "Content-Type: application/json" \
  -d '{
    "description": "Мой первый пост!",
    "imageUrl": "https://example.com/photo.jpg"
  }'
```

### 4. Удаление пользователя (DELETE)
```bash
curl -X DELETE http://localhost:8080/users/Tom
```

## 🔮 Возможные улучшения (To-Do)

Проект является учебным и может быть расширен:
- [ ] **Подключение реальной БД** (H2, PostgreSQL) через Spring Data JPA.
- [ ] **Валидация входных данных** (аннотации `@NotNull`, `@Size`).
- [ ] **Обработка ошибок** (глобальный `@ControllerAdvice` с кастомными исключениями).
- [ ] **Тестирование** (юнит-тесты для сервисов, интеграционные тесты для контроллеров).
- [ ] **Безопасность** (аутентификация и авторизация через Spring Security).
- [ ] **Документация API** (Swagger/OpenAPI).

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).

---
*Проект создан в учебных целях. Вдохновлено статьей "Пишем свой первый сервер на Java и Spring Boot".*
